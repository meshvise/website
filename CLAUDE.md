# Meshvise Vitrine — Developer Guide

## Project Overview

Marketing site for Meshvise (the SCADA/IIoT product), live at https://meshvise.com. Pure static Astro build, deployed via Cloudflare Workers (wrangler). The same Worker also hosts the trial license signer endpoint (`POST /api/trial`) used by the Meshvise product. See cross-repo references below.

## Sister repo and cross-repo context

The Meshvise product app lives in a sister repo at `c:/Users/bruno/Desktop/Meshvise/code/meshvise/` (GitHub: `meshvise/meshvise`), branch `dev`, single worktree, one Claude session at a time (see its own `CLAUDE.md`). **You do not touch that repo.** You only read from it as needed:

- Spec source of truth for the trial license system: `c:/Users/bruno/Desktop/Meshvise/code/meshvise/docs/adr/0006-trial-license-system.md`. Read-only. Defines the JWT claims schema, key isolation rules, expiration sanction, email nurture cadence. Do not improvise off-spec.
- Project status and open work: `c:/Users/bruno/Desktop/Meshvise/code/meshvise/docs/pilotage.md`. The former `checklist.md` task list is frozen in that repo's `docs/archive/`.
- Known cross-repo defects: the audit registry `docs/audits/registre.md` in that repo. **AUD-006**: the product's first boot (`first_boot.py`) requires `issued_to.email`, a claim that ADR-0006 does not define and that `buildTrialClaims` therefore does not set: a trial license issued today never reaches a running interface. The worker follows the spec; the fix belongs to the product or to an ADR-0006 amendment. Do not add the claim to the worker on your own. The cross-repo interfaces are described in that repo's `docs/01-architecture/interfaces-entre-repos.md`.

The trial signer's public key lives in the meshvise repo (verifier side). The trial signer's private key is a Worker secret on this repo (`TRIAL_SIGNING_KEY`), generated by the user from the meshvise-side script and posted via `wrangler secret put`. The private key never lands in this repo, even temporarily, including in tests, fixtures, comments.

## Stack

| Layer | Choice |
|-------|--------|
| Build | Astro 6 (static build into `dist/`) |
| Edge runtime | Cloudflare Workers via wrangler 4 (`src/worker.js`) |
| CSS | Tailwind v4 |
| i18n | French (default) + English. Routes `/<lang>/`. |
| Tests | vitest |

The Worker is currently a 14-line proxy of `env.ASSETS`. Vague 5 will turn it into a proxy plus a `POST /api/trial` route. Stay minimal: do not pile additional logic on the edge unless explicitly scoped.

## Branches and deploy

| Branch | Purpose | Auto-deploy |
|--------|---------|-------------|
| `master` | Prod, serves `meshvise.com` and `www.meshvise.com` | YES (`.github/workflows/deploy.yml`, runs after CI green on master) |
| `dev` | Integration, accumulates work | NO |

**Never push to `master` without explicit user approval.** Master push triggers a Cloudflare Workers deploy to prod after CI completion. Default workflow: PR `dev` to `master`, the user merges when they want a release. Hotfix path: branch from `master`, PR back to `master`, then merge `master` into `dev` to stay aligned.

## Hard rules

- **No em-dashes (long horizontal hyphen)** in code, copy, comments, emails, JSON values. Use colons, parentheses, periods. Applies in all languages, all files.
- **No competitor names in marketing copy**: Niagara, Ignition, EcoStruxure, Tridium, Inductive Automation, Workbench, JACE. Refer to the category ("legacy SCADA platforms", "tag-priced SCADA", etc.).
- **Never write the trial signing private key to disk**, even temporarily, even in tests or fixtures. Read it from `env.TRIAL_SIGNING_KEY` at runtime only. The user's Bitwarden vault is the only canonical store.
- **Never frame shortcuts as laziness.** State the real tradeoff (time-boxing, dependency wait, blast-radius minimization).
- **No phone-home in product traffic.** The `/api/trial` Worker ends with the Resend send. The verifier in the sister repo runs offline. Telemetry, if added later, is opt-in and non-blocking. Do not slip in anonymous pings.

## Workflow

1. New work: branch from `dev`, code, test locally, push the feature branch, PR to `dev`, merge after CI green.
2. Release: PR `dev` to `master` with a release-style title. The user merges; the Worker redeploys automatically.
3. Cross-repo coordination on the trial system: the user is the relay. He pastes a short message in your kickoff or in the conversation when keys, schema, or task scope change on the meshvise side. You do not poll the other repo.
4. TodoWrite for multi-step work. Conventional commits, Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>. Commit after each meaningful step, no batch.

## Quickstart

```bash
npm install                    # one-shot
npm run dev                    # Astro dev server, http://127.0.0.1:4321 (no Worker logic)
npm test                       # vitest
npm run build                  # produces dist/
npx wrangler dev               # local Worker (build dist/ first)
```

To exercise `/api/trial` locally once the route is implemented:

```bash
npm run build && npx wrangler dev
# in another shell:
curl -X POST http://localhost:8787/api/trial \
  -H 'content-type: application/json' \
  -d '{"email":"test@example.com","name":"Test","turnstileToken":"..."}'
```

## Lancer la vitrine

Le serveur de développement est **fixé sur 127.0.0.1:4321**, avec `strictPort`
(cf. `astro.config.mjs`). Deux raisons, toutes deux constatées sur le poste le
2026-09-11 :

- sans `host`, Astro n'écoute qu'en IPv6, donc `localhost:4321` répond et
  `127.0.0.1:4321` non ;
- sans `strictPort`, un second lancement s'installe **en silence** sur 4322 et
  le doublon devient invisible. C'est comme ça que le SPA du produit s'est
  retrouvé servi deux fois.

`npm run dev` est le seul lancement. Plusieurs sessions peuvent le tenter sans
risque : grâce à `strictPort`, le second refuse de démarrer au lieu de
s'installer à côté : un refus veut dire que la vitrine tourne déjà sur 4321.
(`dev.ps1`, à la racine du workspace, jouait ce rôle jusqu'au 2026-09-16.)

Pour juger une page sans serveur de développement, servir le build statique
depuis `dist/`. Ce n'est plus la voie par défaut depuis le 2026-09-11 : le
serveur Astro est le seul mécanisme documenté pour la vitrine.

## Key files

- `src/worker.js` Cloudflare Worker entry (Vague 5 extends it with `/api/trial`)
- `wrangler.jsonc` Worker config, custom domains, bindings (KV, rate limiting, secrets)
- `src/pages/<lang>/index.astro` landing pages (FR default, EN)
- `src/pages/<lang>/trial.astro` trial signup form (Vague 5, à créer)
- `src/components/` Astro components (Hero, Features, Pricing, FAQ, etc.)
- `src/i18n/` translation keys
- `tests/built-html.test.ts` smoke test on the built static output
- `.github/workflows/ci.yml`, `.github/workflows/deploy.yml` CI + auto-deploy


